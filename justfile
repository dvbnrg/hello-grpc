# List all recipes
default:
    @just --list
# Build Client and server binaries
build:
    echo "build called"
# Run all unit tests
test:
    echo "test called"
# Run Local versions of binaries
run:
    echo "run called"